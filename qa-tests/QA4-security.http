### QA4 — SECURITY TEST
### Passport BSides Porto — 14 Abril 2026
### Executado por: Guilherme Pires — Cross-Intel

@baseUrl = https://passport-bsides-porto-production.up.railway.app/api
@eventId = YOUR_EVENT_ID

# ─────────────────────────────────────────
# SEC01 — XSS no nome
# Esperado: 400 ou nome sanitizado (sem script)
# ─────────────────────────────────────────
### SEC01
POST {{baseUrl}}/auth/register
Content-Type: application/json

{
  "name": "<script>alert('xss')</script>",
  "email": "sec01@bsidestest.com",
  "rgpdConsent": true,
  "eventId": "{{eventId}}"
}

###

# ─────────────────────────────────────────
# SEC02 — XSS na empresa
# Esperado: 400 ou empresa sanitizada
# ─────────────────────────────────────────
### SEC02
POST {{baseUrl}}/auth/register
Content-Type: application/json

{
  "name": "Security Test",
  "email": "sec02@bsidestest.com",
  "company": "<img src=x onerror=alert(1)>",
  "rgpdConsent": true,
  "eventId": "{{eventId}}"
}

###

### SEC02-check
GET {{baseUrl}}/auth/resume?token=YOUR_ATTENDEE_TOKEN

# ─────────────────────────────────────────
# SEC03 — SQL Injection no email
# Esperado: 400
# ─────────────────────────────────────────
### SEC03
POST {{baseUrl}}/auth/register
Content-Type: application/json

{
  "name": "Security Test",
  "email": "' OR 1=1--@test.com",
  "rgpdConsent": true,
  "eventId": "{{eventId}}"
}

###

# ─────────────────────────────────────────
# SEC04 — Acesso admin sem JWT
# Esperado: 401
# ─────────────────────────────────────────
### SEC04
GET {{baseUrl}}/admin/dashboard?eventId={{eventId}}

###

### SEC04
GET {{baseUrl}}/admin/dashboard?eventId={{eventId}}

# ─────────────────────────────────────────
# SEC05 — JWT manipulado manualmente
# Esperado: 401
# ─────────────────────────────────────────
### SEC05
GET {{baseUrl}}/admin/dashboard?eventId={{eventId}}
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJmYWtlLWlkLTEyMyIsImVtYWlsIjoiYXR0YWNrZXJAaGFjay5jb20iLCJyb2xlIjoic3RhZmYiLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6OTk5OTk5OTk5OX0.fakesignature

###

# ─────────────────────────────────────────
# SEC06 — Rate limit no registo (6 requests seguidos)
# Esperado: 429 no 6º pedido
# Corre este bloco 6 vezes seguidas rapidamente
# ─────────────────────────────────────────
### SEC06
POST {{baseUrl}}/auth/register
Content-Type: application/json

{
  "name": "Rate Test",
  "email": "sec06.ratelimit.secund@bsidestest.com",
  "rgpdConsent": true,
  "eventId": "{{eventId}}"
}

###

# ─────────────────────────────────────────
# SEC07 — Rate limit no login (6 requests seguidos)
# Esperado: 429 no 6º pedido
# Corre este bloco 6 vezes seguidas rapidamente
# ─────────────────────────────────────────
### SEC07
POST {{baseUrl}}/auth/staff/login
Content-Type: application/json

{
  "email": "attacker@hack.com",
  "password": "wrongpassword"
}

###

# ─────────────────────────────────────────
# SEC08 — Criar staff sem JWT
# Esperado: 401
# ─────────────────────────────────────────
### SEC08
POST {{baseUrl}}/auth/staff/create
Content-Type: application/json

{
  "name": "Fake Staff",
  "email": "fakestaff@hack.com",
  "password": "hacked123"
}
